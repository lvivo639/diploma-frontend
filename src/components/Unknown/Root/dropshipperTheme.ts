import { createMuiTheme } from '@material-ui/core/styles';

const dropshipperTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#264653',
    },
    secondary: {
      main: '#e9c46a',
    },
  },
});
export default dropshipperTheme;
